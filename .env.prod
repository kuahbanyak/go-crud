# Production Environment Variables for Azure SQL Database
# Using your existing Azure SQL Database setup

# Application Configuration
GIN_MODE=release
LOG_LEVEL=info
CONFIG_ENV=prod
SERVER_PORT=8080

# Azure SQL Database Configuration (Production)
# Using your existing Azure SQL Server with a production database
DB_HOST=devdbsql.database.windows.net
DB_PORT=1433
DB_USER=devsql
DB_PASSWORD=Kuahpisah1
DB_DATABASE=sqlprod

# Security (CHANGE THESE FOR PRODUCTION!)
JWT_SECRET=production-jwt-secret-change-this-to-something-very-secure-32chars-minimum
JWT_EXPIRATION=24

# Redis Configuration (using Docker Redis for caching only)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=secure-redis-password-123

# Azure SQL Database SSL Configuration (required for Azure SQL)
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=false
